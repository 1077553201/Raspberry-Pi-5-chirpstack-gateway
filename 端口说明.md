ChirpStack V4 (Docker版) 端口功能速查表端口号协议服务名称功能详细说明谁在使用该端口？1700UDPGateway Bridge网关数据入口。接收树莓派硬件 (lora_pkt_fwd) 发来的加密无线电数据包。你的 global_conf.json 必须指向此端口。SX1302 硬件网关1883TCPMosquittoMQTT 消息总线。系统内部传输数据的核心通道。网关接收到的数据会被转换成 MQTT 消息发布到这里；ChirpStack 也是从这里订阅数据。内部服务、外部MQTT客户端8080TCPChirpStackWeb 管理后台。用于人工登录网页进行设备管理、数据查看、应用配置。同时也负责处理 gRPC-web 请求。浏览器 (你)8090TCPREST APIAPI 接口服务。提供 Swagger UI 文档。你的 Java/Spring Boot 后端如果需要通过代码自动管理设备（增删改查），需调用此接口。后端程序 (Java/Python)5432TCPPostgreSQL持久化数据库。存储用户账号、设备列表、网关配置、应用设置等所有配置信息。ChirpStack 主程序6379TCPRedis高速缓存数据库。存储设备入网的 Session Key（会话密钥）、消息去重记录、下行数据队列。ChirpStack 主程序3001TCPGateway BridgeBasicStation 接口。这是另一种较新的网关协议接口。你的 SX1302 目前使用的是 UDP 协议（1700），所以这个端口目前闲置，但在 Docker 中是开放的。(当前未使用)
